const hero = require(`./acf/hero`)
const row = require(`./acf/row`)
const rowImage = require(`./acf/rowImage`)
const postCarousel = require(`./acf/postCarousel`)

module.exports = {
  /* ACF Blocks */
  hero,
  row,
  rowImage,
  postCarousel,
}
